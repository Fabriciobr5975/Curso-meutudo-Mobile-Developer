import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  compTextInputPersonalizado: {
    width: "100%",
    gap: 10,
    alignItems: "flex-start",
    justifyContent: "center",
  },

  input: {
    width: "100%",
    borderBlockColor: "#000",
    borderRadius: 4,
    borderWidth: 1,
  },

  inputArea: {
    height: 150,
    textAlignVertical: "top",
    width: "100%",
    borderBlockColor: "#000",
    borderRadius: 4,
    borderWidth: 1,
    padding: 10,
  },
});

export default styles;
